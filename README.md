JSON Simulation Server

A simple JSON Server–based backend for managing simulations and uploading files.
Uses json-server, multer, and local file storage.

* Manage simulations (add, update, delete, toggle running state)
* Upload files per simulation
* Store uploaded files under /uploads
* Data persisted in db.json
